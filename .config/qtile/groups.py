from libqtile.config import Group

class Groups:

	def init_groups(self):
		return [
			Group(
				name = "HOME",
			),
			Group(
				name = "ONE",
				init = False,
			),
			Group(
				name = "TWO",
				init = False,
			),
			Group(
				name = "THREE",
				init = False,
			),
			Group(
				name = "FOUR",
				init = False,
			),
			Group(
				name = "FIVE",
				init = False,
			),
			Group(
				name = "SIX",
				init = False,
			),
			Group(
				name = "SEVEN",
				init = False,
			),
			Group(
				name = "EIGHT",
				init = False,
			),
			Group(
				name = "NINE",
				init = False,
			),
			Group(
				name = "ZERO",
				init = False,
			),		
		]
